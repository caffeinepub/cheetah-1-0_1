{
  "frontend/src/App.tsx": "Main application component with panic mode state management — activates a full-viewport white overlay for 60 seconds via button click or Cmd/Ctrl+P/B keyboard shortcuts, with the footer copyright removed and panic callback passed to Header.",
  "frontend/src/pages/CodeEditorPage.tsx": "Code Editor page container with three switchable sub-tabs (Code Editor, Presentation Builder, Website Builder) rendered via internal tab state.",
  "frontend/src/pages/QuickOpen.tsx": "Quick Open page displaying six large branded cards (TikTok, YouTube, Wikipedia, Disney+, ChatGPT, Gemini) with brand accent colors and glow effects, each opening the site in a proxy iframe tab. Replaces the missing `SiDisneyplus` icon with a custom inline \"D+\" text component.",
  "frontend/src/components/codeeditor/WebsiteBuilderTab.tsx": "Website Builder sub-tab with a component palette (header, text, image, button), a layer list with reorder/delete controls, a live canvas preview, a properties panel for editing selected components, and a fullscreen iframe preview mode.",
  "frontend/src/components/common/ProxyInfoPanel.tsx": "A toggleable proxy info panel component that displays Cheetah 1.0's CORS proxy configuration details (allorigins.win route pattern, port 8000 forwarding reference) with expandable accordion sections, styled with the amethyst/purple glassmorphism theme. Also exports a `ProxyInfoButton` trigger component with active/inactive visual states.",
  "frontend/src/pages/HomeSearch.tsx": "Home/search page with address bar, proxy iframe, and new tab page — renders the iframe immediately with no loading overlay, shows the active proxy name in the toolbar, and displays error/new-tab pages as appropriate.",
  "frontend/src/components/layout/Header.tsx": "App header with logo, proxy browser label, SECURE badge, and a red-glowing PANIC button that accepts an `onPanic` callback prop and triggers the panic overlay.",
  "frontend/src/hooks/useTabs.ts": "Tab state management hook with `isLoading` fully removed from the `Tab` interface and all state updates; `setTabLoading` is kept as a no-op for API compatibility, and `hasError` / proxy fallback logic are preserved.",
  "frontend/src/components/tabs/TabContent.tsx": "Tab iframe renderer that shows the iframe immediately with no loading overlay or spinner; preserves the empty new-tab page and the error state UI when all proxies fail.",
  "frontend/src/components/tabs/TabBar.tsx": "Tab bar component that renders all open tabs with a Globe icon (no loading spinner), active state styling, close buttons, and a new-tab button."
}