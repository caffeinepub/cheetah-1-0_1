{
  "frontend/src/App.tsx": "Main app component updated to import and render the ProxyLinks page when activePage === 'proxy', passing the handleOpenUrl callback.",
  "frontend/src/pages/CodeEditorPage.tsx": "Code Editor page container with three switchable sub-tabs (Code Editor, Presentation Builder, Website Builder) rendered via internal tab state.",
  "frontend/src/pages/QuickOpen.tsx": "Quick Open page displaying six large branded cards (TikTok, YouTube, Wikipedia, Disney+, ChatGPT, Gemini) with brand accent colors and glow effects, each opening the site in a proxy iframe tab. Replaces the missing `SiDisneyplus` icon with a custom inline \"D+\" text component.",
  "frontend/src/components/codeeditor/WebsiteBuilderTab.tsx": "Website Builder sub-tab with a component palette (header, text, image, button), a layer list with reorder/delete controls, a live canvas preview, a properties panel for editing selected components, and a fullscreen iframe preview mode.",
  "frontend/src/components/common/ProxyInfoPanel.tsx": "Toggleable proxy info panel displaying Cheetah 1.0's CORS proxy configuration with allorigins.win as the primary provider, showing the updated route pattern (`api.allorigins.win/raw?url=`), status indicator, and expandable accordion sections styled in the amethyst/purple glassmorphism theme.",
  "frontend/src/pages/HomeSearch.tsx": "Home/search page with address bar and proxy iframe; proxy label array updated so index 0 shows `allorigins.win` in the address bar badge, with direct/search-engine detection unchanged.",
  "frontend/src/components/layout/Header.tsx": "App header with logo, proxy browser label, SECURE badge, and a red-glowing PANIC button that accepts an `onPanic` callback prop and triggers the panic overlay.",
  "frontend/src/hooks/useTabs.ts": "Tab state management hook with proxy fallback chain; the primary proxy is now `api.allorigins.win/raw?url=` (allorigins.win), with `isSearchEngineUrl` and `DIRECT_PROXY_INDEX` sentinel so search queries bypass all proxy wrapping and load directly.",
  "frontend/src/components/tabs/TabContent.tsx": "Tab iframe renderer that now imports `isSearchEngineUrl` and skips proxy retry cycling for search engine URLs on iframe error, ensuring search results always load directly without proxy interference.",
  "frontend/src/components/tabs/TabBar.tsx": "Tab bar component that renders all open tabs with a Globe icon (no loading spinner), active state styling, close buttons, and a new-tab button.",
  "frontend/src/pages/Games.tsx": "Games page updated to accept an onNavigate prop, adds a 'Cheetah Education Hub' card at the top of the grid that navigates internally to the Education page instead of opening a proxy URL, and uses site.name as the key instead of site.url.",
  "frontend/src/pages/Education.tsx": "Education Hub page with a hero banner, Ed AI chat (subject selector + question input via window.puter.ai.chat), and a Challenge Yourself quiz section with 28 hardcoded multiple-choice questions across Math, ELA, Science, History, and CS. Fixed the lint error by removing the empty `EdAIChatProps` interface and the unused `QuizState` interface/`LoadingSpinner` import, and replaced all `flex-shrink-0` with `shrink-0`.",
  "frontend/src/components/layout/Navigation.tsx": "Sidebar navigation updated to include a seventh 'Proxy Links' item (Globe icon) with PageId type now including 'proxy'.",
  "frontend/src/pages/ProxyLinks.tsx": "New Proxy Links page displaying three proxy service cards (ProxySite, ProxyOrb, ProxyProxy) with numbered cards, descriptions, and Launch buttons; ProxyProxy is visually distinguished as the primary/in-use proxy with a purple glow border, star badge, and green \"In Use\" indicator."
}